<div id="header">
	<%= render "header" %>
</div>

<div class="container">
	<div class="row">
		<div class="col-xs-3", style="background-color: rgba(0, 0, 0, 0.1); height: 600px;">
			<div class="profile">
				<table class='table'>
					<br>
      				<tr><%= attachment_image_tag(@user, :image, :fill, 150, 100, fallback: "no_image.jpg", size:'150×150') %></tr> 
      				<tr>
    	  				<th>name</th>
    	  				<th><%= @user.name %></th>
      				</tr>
      				<tr>
			    	  <th>introduction</th>
			    	  <th><%= @user.introduce %></th>
				    </tr>
      				<tr>
			    	  <th>level</th>
			    	  <th><%= @user.level %></th>
				    </tr>
      				<tr>
			    	  <th>モニュメント</th>
				    </tr>
				</table>
				<div class='row'>
  					<%= link_to '',edit_user_path(@user),class: "col-xs-10 btn btn-default glyphicon glyphicon-wrench edit_user_#{@user.id}" %>
				</div>
			</div>
		</div>

		<div class="col-xs-9", style="background-color: rgba(255, 10, 55, 0.1); height: 1000px;">
			<br>
			<div class="main contents">
					<% @project_for_graph.each do |project_id , data| %>
						<div class="col-xs-5">
							<%= link_to project_path(project_id) do %>
								<%= @title_for_graph[project_id] %>
								<%= column_chart data %>
							<% end %>
						</div>
					<% end %>

			</div>

		</div>
	</div>
</div>
